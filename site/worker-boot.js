// Загрузчик потока для страницы.
//
// В расширении поток собирается из blob: внутри чужого фрейма иначе никак.
// Здесь всё своё, и файлы подключаются напрямую — сам worker.js при этом
// остаётся тем же, что и в расширении.
importScripts('pixelmatch.js', 'compare.js', 'worker.js');
